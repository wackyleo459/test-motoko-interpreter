---
sidebar_position: 1
---

# Motoko Tutorial Intro

Let's discover **Motoko language in less than 5 minutes**.

## Getting Started

```js
console.log("Every repo must come with a mascot.");
```

```motoko
1 + 2
```

```motoko
actor Counter {

  var value = 1;

  public func inc() : async Nat {
    value += 1;
    return value;
  };
};
```

```motoko
import Array "mo:base/Array";
import Nat "mo:base/Nat";
let xs = [4, 2, 6];
Array.sort(xs, Nat.compare);
```

```candid
service : {
  inc : () -> ();
  get : () -> (nat) query;
}
```

<h2>Single line comments &amp; regexes</h2>
<pre>
  <code>// http://lea.verou.me var comment = /\/\*[\w\W]*?\*\//g;</code>
</pre>

<h2>Link in comment</h2>
<pre>
  <code>// http://lea.verou.me /* http://lea.verou.me */</code>
</pre>

<h2>Nested strings</h2>
<pre>
  <code>var foo = "foo", bar = "He \"said\" 'hi'!"</code>
</pre>

<!-- # jsx inside

export const RunInput = ({ children, color }) => (
  <div
    style={{
      backgroundColor: color,
      height: "5rem",
      color: "white",
    }}
  >
    {children}
  </div>
);

<RunInput color="green">Hello</RunInput> -->

# Dummy Run Button

export const RunButton = ({ input, children }) => (
  <div>
    <button onClick={() => console.log(input)}>Run {input}</button>
    {children}
  </div>
);

<RunButton input="Motoko">hello</RunButton>;
